# mvn clean = effacer historique
# mvn compile = compiler
# mvn test = tester
# mvn package = packager (dépendance d'un autre projet)
# mvn install = packager + dépendance maven
# mvn clean install = clean + compile + test + package +install
